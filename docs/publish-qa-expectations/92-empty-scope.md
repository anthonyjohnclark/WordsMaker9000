# Publish QA 92 — Expected Failure: Empty Scope

The Publish modal should open. After choosing any format and destination, preflight should block with `PUBLISH_EMPTY_SCOPE`. No successful manifest or destination artifact should remain.
